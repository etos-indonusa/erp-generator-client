import { TextStringPipe } from './text-string.pipe';

describe('TextStringPipe', () => {
  it('create an instance', () => {
    const pipe = new TextStringPipe();
    expect(pipe).toBeTruthy();
  });
});
