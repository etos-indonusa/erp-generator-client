/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CustomFieldValueDto } from '../../models/custom-field-value-dto';

export interface CustomFieldValueControllerUpdate$Params {
  id: string;
      body: CustomFieldValueDto
}

export function customFieldValueControllerUpdate(http: HttpClient, rootUrl: string, params: CustomFieldValueControllerUpdate$Params, context?: HttpContext): Observable<StrictHttpResponse<{
'code'?: number;
'pesan'?: string;
'data'?: CustomFieldValueDto;
}>> {
  const rb = new RequestBuilder(rootUrl, customFieldValueControllerUpdate.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<{
      'code'?: number;
      'pesan'?: string;
      'data'?: CustomFieldValueDto;
      }>;
    })
  );
}

customFieldValueControllerUpdate.PATH = '/nonauth/custom_field_value/custom_field_value/{id}';
