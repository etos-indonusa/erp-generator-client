export declare const TokenOnlyResponseFormSchema: {
    access_token: {
        default: string;
        required: boolean;
    };
    refresh_token: {
        default: string;
        required: boolean;
    };
};
